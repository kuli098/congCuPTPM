const style = theme => ({
  bottomLine: {
    width: '3rem',
    background: theme.palette.primary.main,
    height: '0.1875rem',
    margin: '0 auto'
  },
  textCate: { fontWeight: 'bold', marginBottom: '1.5rem' }
});

export default style;
