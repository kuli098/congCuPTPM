import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#7986cb',
      main: '#00cb54',
      dark: '#00983F',
      contrastText: '#fff'
    }
  },
});

export default theme;
